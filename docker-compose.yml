services:
  web:
    build: .
    ports:
      - "5001:5000"
    environment:
      - SECRET_KEY=your-secret-key-here
      - DATABASE_URL=sqlite:///tasks.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
